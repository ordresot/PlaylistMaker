<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/activity_main_color"
    android:orientation="vertical"
    android:fitsSystemWindows="true">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/activity_main_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:title="@string/activity_main_name"
        app:titleTextAppearance="@style/ActivityMainToolbarAppearance"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/button_search"

        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginHorizontal="@dimen/regular_horizontal_indent"
        android:layout_weight="3"
        android:backgroundTint="@color/white"
        android:foreground="?attr/selectableItemBackground"
        android:textAllCaps="false"
        android:textColor="@color/activity_main_text_color"
        android:textSize="@dimen/headers_text_size"
        app:cornerRadius="16dp"
        app:iconGravity="textStart"
        app:iconPadding="8dp"
        app:iconTint="@color/activity_main_text_color"

        android:text="@string/button_search_text"
        app:icon="@drawable/ic_search"
        android:layout_marginVertical="@dimen/regular_vertical_indent"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/button_library"

        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginHorizontal="@dimen/regular_horizontal_indent"
        android:layout_weight="3"
        android:backgroundTint="@color/white"
        android:foreground="?attr/selectableItemBackground"
        android:textAllCaps="false"
        android:textColor="@color/activity_main_text_color"
        android:textSize="@dimen/headers_text_size"
        app:cornerRadius="16dp"
        app:iconGravity="textStart"
        app:iconPadding="8dp"
        app:iconTint="@color/activity_main_text_color"

        android:text="@string/button_library_text"
        app:icon="@drawable/ic_library"
        android:layout_marginBottom="@dimen/regular_vertical_indent"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/button_settings"

        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginHorizontal="@dimen/regular_horizontal_indent"
        android:layout_weight="3"
        android:backgroundTint="@color/white"
        android:foreground="?attr/selectableItemBackground"
        android:textAllCaps="false"
        android:textColor="@color/activity_main_text_color"
        android:textSize="@dimen/headers_text_size"
        app:cornerRadius="16dp"
        app:iconGravity="textStart"
        app:iconPadding="8dp"
        app:iconTint="@color/activity_main_text_color"

        android:text="@string/activity_settings_name"
        app:icon="@drawable/ic_settings"
        android:layout_marginBottom="@dimen/bold_vertical_indent"/>
</LinearLayout>